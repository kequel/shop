<VirtualHost *:80>
    ServerName localhost
    ServerAlias *.localhost
    DocumentRoot /var/www/html

    # Przekierowanie HTTP na HTTPS
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://localhost:19662$1 [R=301,L]
</VirtualHost>
